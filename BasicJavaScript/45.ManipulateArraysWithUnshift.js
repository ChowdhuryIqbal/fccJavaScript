/*
    .unshift() is opposite to .push()
    .unshift() inserts element at the begining of an array
*/

// Setup
var myArray = [["John", 23], ["dog", 3]];
myArray.shift();
myArray.unshift(["Paul", 35]);

// Only change code below this line